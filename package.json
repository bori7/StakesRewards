{
  "scripts": {
    "start": "cd client && npm run start",
    "build": "cd client && npm run startd",
    "test": "cd client && npm run test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.5.0",
    "@truffle/hdwallet-provider": "^2.0.4",
    "dotenv": "^16.0.0"
  }
}
